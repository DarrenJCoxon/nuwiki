/**
 * `@nusoft/nuwiki/agent-tools` — helpers for agents that read NuWiki.
 *
 * Skeleton only. Concrete helpers (cite, traverse, summarise) are post-v0.1.0.
 */

import { NotImplementedError } from './errors.js';

export const NuWikiAgentTools = {
  cite: (): never => {
    throw new NotImplementedError('NuWikiAgentTools.cite', 'post-v0.1.0');
  },
  traverse: (): never => {
    throw new NotImplementedError('NuWikiAgentTools.traverse', 'post-v0.1.0');
  },
  summarise: (): never => {
    throw new NotImplementedError('NuWikiAgentTools.summarise', 'post-v0.1.0');
  },
};
